# Production Environment Configuration
# Copy this file to .env on the production server and fill in the values

# Server
NODE_ENV=production
PORT=3001

# Database (MySQL/MariaDB)
# Get these values from ISPConfig → Sites → Databases
DB_HOST=localhost
DB_USER=project_user
DB_PASSWORD=CHANGE_THIS_TO_ISPCONFIG_DATABASE_PASSWORD
DB_NAME=project_tracker

# JWT Authentication
# Generate a secure random secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHARACTER_SECRET_KEY_USE_CRYPTO_RANDOMYTES
JWT_EXPIRES_IN=7d

# CORS
# Set this to your actual domain name
CORS_ORIGIN=https://project-tracker.example.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Production Notes:
# 1. NEVER commit this file to git with real credentials
# 2. Keep the .env file in ~/private/backend/ (not in web root)
# 3. Set file permissions: chmod 600 .env
# 4. Generate a strong JWT_SECRET using the command above
# 5. Update CORS_ORIGIN to match your domain exactly
